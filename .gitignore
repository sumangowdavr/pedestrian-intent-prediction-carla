# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.env

# ML weights - too large; users download separately
*.pt
*.pth
*.h5
*.onnx

# Raw CARLA dataset (hundreds of MB); users download separately
/data/

# Left-over from the pre-flatten layout; the subdir keeps venv + weights + demo cache locally
pedestrian_intent_prediction/

# Regenerable intermediate outputs (kept locally, not committed)
demo/merged_images/
demo/merged_visual_flow_final/
demo/merged_visual_intent_final/
demo/*.gif

real_run/merged_img/
real_run/vis_flow/
real_run/vis_tracker/
real_run_sanity/

# YOLO training run artifacts
runs/

# IDE / OS junk
.vscode/
.idea/
.DS_Store
# LibreOffice lock files
.~lock.*#
