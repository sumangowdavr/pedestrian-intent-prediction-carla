# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.env

# ML weights - too large; users download separately
*.pt
*.pth
*.h5
*.onnx

# Raw CARLA dataset (hundreds of MB); users download separately
/data/

# Left over from the pre-flatten layout; still holds local venv + weights + data
pedestrian_intent_prediction/

# Regenerable intermediate outputs (kept locally, not committed)
outputs/demo/merged_images/
outputs/demo/merged_visual_flow_final/
outputs/demo/merged_visual_intent_final/
outputs/demo/*.gif

outputs/real_run/merged_img/
outputs/real_run/vis_flow/
outputs/real_run/vis_tracker/
outputs/real_run_sanity/

# YOLO training-run artifacts
runs/

# IDE / OS junk
.vscode/
.idea/
.DS_Store
# LibreOffice lock files
.~lock.*#

# Editable report source; the committed PDF is the shareable version
docs/*.docx
